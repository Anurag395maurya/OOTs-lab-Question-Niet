Sketch a class diagram containing class called Employee which models an employee with an ID , name and salary Add method  raise salary (percent )that  the increase the salary by the given percentage.
public class Employee {
    private int id;
    private String name;
    private double salary;
    public Employee(int id, String name, double salary) {
        this.id = id;
        this.name = name;
        this.salary = salary;
    }
    public int getId() {
        return id;
    }
    public String getName() {
        return name;
    }
    public double getSalary() {
        return salary;
    }
    public void setSalary(double salary) {
        this.salary = salary;
    }
    public void raiseSalary(double percent) {
        if (percent > 0) {
            salary += salary * percent / 100.0;
        }
    }
    public String toString() {
        return "Employee[id=" + id + ", name=" + name + ", salary=" + salary + "]";
    }
    public static void main(String[] args) {
        Employee e = new Employee(1, "Anurag", 5000);
        System.out.println(e);  
        e.raiseSalary(10);     
        System.out.println(e);  
    }
}
